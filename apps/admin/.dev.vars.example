# Copy to .dev.vars for `pnpm dev`. In production set secrets with `wrangler secret put <NAME>`.
BOT_TOKEN=123456:replace-me
GITHUB_DISPATCH_TOKEN=
# Local only: skips auth for requests to localhost / 127.0.0.1.
ADMIN_DEV_BYPASS=1
